## Project Overview

TaskFlow is a RESTful Web API designed for personal task management. Users can securely register, authenticate using JWT tokens, and manage their own tasks. Administrators have elevated permissions to view, update, and delete any task in the system.

The application demonstrates authentication, authorization, database management, testing, and cloud deployment using modern ASP.NET Core technologies.